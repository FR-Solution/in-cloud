
import CodeBlock                from '@theme/CodeBlock'
import { CERTIFICATES }         from '@site/src/constants/kubernetes/certs'
import { PORTS }                from '@site/src/constants/kubernetes/ports'
import { CUSTOM_VALUE }         from '@site/src/constants/kubernetes/customValue'
import TabItem                  from '@theme/TabItem'
import Tabs                     from '@theme/Tabs'
import dedent                   from 'dedent'
import { COMPONENTS_VERSION }   from '@site/src/constants/kubernetes/componentsVersion'

export const ETCD_endpoints           = 'https://10.10.10.3:2379,https://10.10.10.4:2379,https://10.10.10.5:2379'
export const ETCD_endpoint_1          = 'https://10.10.10.3:2379'
export const ETCD_endpoint_2          = 'https://10.10.10.4:2379'
export const ETCD_endpoint_3          = 'https://10.10.10.5:2379'
export const ETCD_etcd_pod_1          = 'etcd-bcloud-k8s-sandbox-dev-csko-1-tcgzn-7plhz'
export const ETCD_etcd_pod_2          = 'etcd-bcloud-k8s-sandbox-dev-csko-1-tcgzn-h9g49'
export const ETCD_etcd_pod_3          = 'etcd-bcloud-k8s-sandbox-dev-csko-1-tcgzn-wps6k'
export const ETCD_k8s_node_1          = 'bcloud-k8s-sandbox-dev-csko-1-tcgzn-7plhz'
export const ETCD_k8s_node_2          = 'bcloud-k8s-sandbox-dev-csko-1-tcgzn-h9g49'
export const ETCD_k8s_node_3          = 'bcloud-k8s-sandbox-dev-csko-1-tcgzn-wps6k'
export const ETCD_k8s_node_address_1  = '10.10.10.3'
export const ETCD_k8s_node_address_2  = '10.10.10.4'
export const ETCD_k8s_node_address_3  = '10.10.10.5'
export const ETCD_api_address         = '10.10.10.2'
export const ETCD_k8s_cluster_name    = 'bcloud-k8s-sandbox-test-csko-1'
export const ETCD_base_k8s_path       = CUSTOM_VALUE.kuberneteBaseFolderPath.value
export const ETCD_peer_cert_path      = CERTIFICATES.etcdPeer.crtPath
export const ETCD_peer_key_path       = CERTIFICATES.etcdPeer.keyPath
export const ETCD_ca_cert_path        = CERTIFICATES.etcdCA.crtPath
export const ETCD_ca_key_path         = CERTIFICATES.etcdCA.keyPath
export const ETCD_serv_cert_path      = CERTIFICATES.etcdServer.crtPath
export const ETCD_serv_key_path       = CERTIFICATES.etcdServer.keyPath
export const ETCD_version             = 'v3.5.17'
export const ETCD_workdir             = '/tmp/etcd'
export const ETCD_data_dir            = '/var/lib/etcd'
export const ETCD_db_path             = '/var/lib/etcd/member/snap/db'
export const ETCD_serv_port           = PORTS.etcdServer.portNumber
export const ETCD_peer_port           = PORTS.etcdPeer.portNumber
export const ETCD_metr_port           = PORTS.etcdMetricServer.portNumber



## Создание резервных копий

### Создание мгновенных снимков

><Tabs groupId="etcd-alias">
>  <TabItem value='etcdctl'>
>    Наиболее рекомендуемый метод. Позволяет сохранить дамп за пределами контейнера ETCD.
>
>    <CodeBlock language="bash">{`etcdctl --endpoints=\${ENDPOINTS} snapshot save PATH_TO_SAVE/backup.db`}</CodeBlock>
>
>  </TabItem>
>  <TabItem value='ectl'>
>    Рабочий вариант, когда нет исполняемого файла под рукой. Дамп будет сохранен внутри
>    файловой системы контейнера. Поэтому рекомендуется указывать путь в примонтированные области.
>
>    <CodeBlock language="bash">{`ectl snapshot save ${ETCD_data_dir}/backup.db`}</CodeBlock>
>
>    :::note
>    В кластерах B-Cloud файлы ETCD по умолчанию хранятся на мастере по одноименному пути <code>{ETCD_data_dir}</code>
>    :::
>
>    Теперь на хостовой машине вы можете скопировать файл <code>{ETCD_data_dir}/backup.db</code>
>    в другое место, чтобы сохранить его. Например:
>
>    <CodeBlock language="bash">{`cp ${ETCD_data_dir}/backup.db ${ETCD_workdir}/backup.db`}</CodeBlock>
>
>  </TabItem>
>  <TabItem value='nectl'>
>    TODO Описать процесс с пробросом mount (положить бекап).
>  </TabItem>
>  <TabItem value='kectl'>
>    Нерекомендуемый вариант. Т.к. файл будет сохранен внутри контейнера и даже если сохранить в
>    примонтированную на хост директорию, все равно придется подключаться к ВМ, чтобы скопировать файл.
>
>    <CodeBlock language="bash">{`kectl snapshot save ${ETCD_data_dir}/backup.db`}</CodeBlock>
>
>    :::note
>    В кластерах B-Cloud файлы ETCD по умолчанию хранятся на мастере по одноименному пути <code>{ETCD_data_dir}</code>
>    :::
>
>    Теперь на хостовой машине вы можете скопировать файл <code>{ETCD_data_dir}/backup.db</code> в другое место, чтобы сохранить его. Например:
>
>    <CodeBlock language="bash">{`cp ${ETCD_data_dir}/backup.db ${ETCD_workdir}/backup.db`}</CodeBlock>
>
>  </TabItem>
></Tabs>

*****

### Создание резервной копии файлов БД

Основной файл БД ETCD кластера расположен по пути <code>{ETCD_db_path}</code>

В случае, если нет возможности для создания мгновенного снимка можно создать резервную копию
файла БД <code>{ETCD_db_path}</code> или целиком дериктории <code>{ETCD_data_dir}</code>:

<CodeBlock language="bash">
{`cp -r ${ETCD_db_path} ${ETCD_workdir}/db
  cp -r ${ETCD_data_dir} ${ETCD_workdir}/backup
`}
</CodeBlock>

*****

## Восстановление кластера

### Восстановление с одного узла из мгновенного снимка

Данный вариант подразумевает, что бэкап разворачивается на первом узле и кластер запускается в режиме одного узла.
Далее поочередно будут добавляться другие узлы кластера.
Таким образом, если адрес и имя первого узла фигурируют в конфигурации других все еще работающих узлов,
но еще недобавленных в метаданные восстановленной базы данных
они незамедлительно упадут с ошибкой и приостановят свою работу уступив место новому кластеру.

><Tabs groupId="etcd-node">
>  <TabItem value='Master-1'>
>    1. Загрузите файл мгновенного снимка на ВМ первого узла кластера по пути <code>{ETCD_workdir}/backup.db</code> при помощи rsync
>
>    <CodeBlock language="bash">
>{`ssh capv@$\{K8S_NODE_ADDRESS_1} "mkdir -p ${ETCD_workdir}"
>  rsync --rsync-path="sudo rsync" \\
>    LOCAL_PATH_TO_BACKUP/backup.db \\
>    capv@$\{K8S_NODE_ADDRESS_1}:${ETCD_workdir}/backup.db
>`}
>    </CodeBlock>
>
>    2. Подключитесь к ВМ первого узла кластера по ssh и переключитесь на суперпользователя.
>    Затем остановите `kubelet`, чтобы не позволить ему запускать ETCD.
>
>    <CodeBlock language="bash">{`systemctl stop kubelet`}</CodeBlock>
>
>    3. Остановите и удалите контейнер `etcd`:
>
>    <CodeBlock language="bash">
>{`export CONTAINER_ID="$(crictl ps -a  \\
>    --label io.kubernetes.container.name=etcd \\
>    --label io.kubernetes.pod.namespace=kube-system \\
>    | awk 'NR>1{r=$1} $0~/Running/{exit} END{print r}')"
>  crictl stop \${CONTAINER_ID}
>  crictl rm \${CONTAINER_ID}
>`}
>    </CodeBlock>
>
>    4. Создайте [резервную копию](#создание-резервной-копии-файлов-бд).
>
>    5. Удалите старые файлы из директории <code>{ETCD_data_dir}</code>
>
>    <CodeBlock language="bash">{`rm -rf ${ETCD_data_dir}/*`}</CodeBlock>
>
>    6. Подготовьте БД из снимка:
>    <CodeBlock language="bash">
>{`etcdctl snapshot restore ${ETCD_workdir}/backup.db \\
>    --name=\${K8S_NODE_1} \\
>    --data-dir ${ETCD_data_dir} \\
>    --initial-advertise-peer-urls=https://\${K8S_NODE_ADDRESS_1}:2380 \\
>    --initial-cluster=\${K8S_NODE_1}=https://\${K8S_NODE_ADDRESS_1}:2380
>`}
>    </CodeBlock>
>
>    :::note
>    Обратите внимание, что запускается локальный файл `etcdctl` и снимок так же расположен на мастере в директории <code>{ETCD_workdir}</code>.
>    В качестве `data-dir` указана директория, которая монтируется внутрь контейнера ETCD и будет использоваться в качестве хранения файлов БД.
>    :::
>
>    7. Проверьте и по необходимости отредактируйте параметры запуска ETCD в файле <code>${ETCD_base_k8s_path}/manifests/etcd.yaml</code>
>    указанные в разделе `spec.containers[0].command`.
>    А именно проверьте наличие необходимых полей и их значения, поля не входящий в данный перечень удалять не нужно!
>
>    <CodeBlock language="bash">
>{`--advertise-client-urls=${ETCD_endpoint_1}
>  --cert-file=${ETCD_serv_cert_path}
>  --client-cert-auth=true
>  --data-dir=${ETCD_data_dir}
>  --key-file=${ETCD_serv_key_path}
>  --listen-client-urls=https://127.0.0.1:${ETCD_serv_port},${ETCD_endpoint_1}
>  --listen-metrics-urls=http://0.0.0.0:${ETCD_metr_port}
>  --listen-peer-urls=https://${ETCD_k8s_node_address_1}:${ETCD_peer_port}
>  --name=${ETCD_k8s_node_1}
>  --peer-cert-file=${ETCD_peer_cert_path}
>  --peer-client-cert-auth=true
>  --peer-key-file=${ETCD_peer_key_path}
>  --peer-trusted-ca-file=${ETCD_ca_cert_path}
>  --trusted-ca-file=${ETCD_ca_key_path}
>`}
>    </CodeBlock>
>
>    <table>
>      <tr><th>Параметр</th><th>Описание</th></tr>
>      <tr><td>`advertise-client-urls` </td><td>Анонсируемые точки подключения к узлу, для подключения клиентов. Совпадает с IP адресом узла k8s кластера</td></tr>
>      <tr><td>`cert-file`             </td><td>Путь до сертификата ETCD кластера</td></tr>
>      <tr><td>`client-cert-auth`      </td><td>Включить/выключить авторизацию пользователей по сертификату</td></tr>
>      <tr><td>`data-dir`              </td><td>Директория в которой хранятся файлы БД ETCD</td></tr>
>      <tr><td>`key-file`              </td><td>Путь до ключа сертификата ETCD кластера</td></tr>
>      <tr><td>`listen-client-urls`    </td><td>Точка подключения, на которой ETCD будет принимать клиентские запросы (`0.0.0.0`, `127.0.0.1` или IP адрес узла k8s кластера)</td></tr>
>      <tr><td>`listen-metrics-urls`   </td><td>Точка подключения, на которой ETCD будет принимать запросы к метрикам (`0.0.0.0`, `127.0.0.1` или IP адрес узла k8s кластера)</td></tr>
>      <tr><td>`listen-peer-urls`      </td><td>Точка подключения, на которой ETCD будет принимать запросы от других узлов ETCD кластера (`0.0.0.0`, `127.0.0.1` или IP адрес узла k8s кластера)</td></tr>
>      <tr><td>`name`                  </td><td>Имя узла ETCD кластера</td></tr>
>      <tr><td>`peer-cert-file`        </td><td>Путь до сертификата для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`peer-client-cert-auth` </td><td>Путь до сертификата для авторизации подключений между узлами ETCD кластера</td></tr>
>      <tr><td>`peer-key-file`         </td><td>Путь до ключа сертификата для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`peer-trusted-ca-file`  </td><td>Путь до корневого доверенного сертификата для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`trusted-ca-file`       </td><td>Путь до корневого доверенного сертификата ETCD кластера</td></tr>
>    </table>
>
>    :::note
>    Если параметр `initial-cluster-state` равен значению "existing" или отсутствуют файлы БД и метаданных в ней,
>    тогда все прочие параметры, имя которых начинается со слова `initial` будут игнорироваться.
>    В нашем случае, мы уже восстановили файлы БД и внесли метаданные на этапе подготовки БД из снимка.
>    :::
>
>    8. Запустите kubelet:
>
>    <CodeBlock language="bash">{`systemctl start kubelet`}</CodeBlock>
>
>    9. Проверьте состояние контейнера и наличие ошибок
>
>    <CodeBlock language="bash">
>{`export CONTAINER_ID="$(crictl ps -a  \\
>    --label io.kubernetes.container.name=etcd \\
>    --label io.kubernetes.pod.namespace=kube-system \\
>    | awk 'NR>1{r=$1} $0~/Running/{exit} END{print r}')"
>
>  crictl ps --id $CONTAINER_ID
>
>  crictl logs $CONTAINER_ID
>`}
>    </CodeBlock>
>
>    10. Повторите этапы сбора [пререквизитов](#подготовка-окружения) и [подключения](#подключение-к-etcd) к кластеру ETCD только для текущего узла.
>
>    11. При помощи команд `member list` и `endpoint status` описанных выше [проверьте состояние кластера](#проверка-подключения-и-состояния-кластера).
>
>    12. переходите к настройке второго узла.
>
>  </TabItem>
>  <TabItem value='Master-2'>
>
>    1. На первом узле ETCD кластера добавьте второй узел кластера в режиме обучающегося:
>
>    <CodeBlock language="bash">{`etcdctl member add \${K8S_NODE_2} --peer-urls=https://\${K8S_NODE_ADDRESS_2}:2380 --learner ;`}</CodeBlock>
>
>    2. Подключитесь к ВМ  второго узла кластера по ssh и остановите `kubelet`, чтобы не позволить ему запускать ETCD.
>
>    <CodeBlock language="bash">{`systemctl stop kubelet`}</CodeBlock>
>
>    3. Остановите и удалите контейнер `etcd`:
>
>    <CodeBlock language="bash">
>{`export CONTAINER_ID="$(crictl ps -a  \\
>    --label io.kubernetes.container.name=etcd \\
>    --label io.kubernetes.pod.namespace=kube-system \\
>    | awk 'NR>1{r=$1} $0~/Running/{exit} END{print r}')"
>  crictl stop \${CONTAINER_ID}
>  crictl rm \${CONTAINER_ID}
>`}
>    </CodeBlock>
>
>    4. Создайте [резервную копию](#создание-резервной-копии-файлов-бд).
>
>    5. Удалите старые файлы из директории <code>{ETCD_data_dir}</code>
>
>    <CodeBlock language="bash">{`rm -rf ${ETCD_workdir}/*`}</CodeBlock>
>
>    6. Откройте на редактирование файл <code>${ETCD_base_k8s_path}/manifests/etcd.yaml</code>:
>    Найдите и замените значение следующих ключей в разделе `spec.containers[0].command` аналогично примеру ниже
>    в соответствии с вашими параметрамии таблицей с описанием:
>
>    <CodeBlock language="bash">
>{`--advertise-client-urls=${ETCD_endpoint_2}
>  --cert-file=${ETCD_serv_cert_path}
>  --client-cert-auth=true
>  --data-dir=${ETCD_data_dir}
>  --initial-advertise-peer-urls=https://${ETCD_k8s_node_address_2}:${ETCD_peer_port}
>  --initial-cluster=${ETCD_k8s_node_1}=https://${ETCD_k8s_node_address_1}:${ETCD_peer_port},${ETCD_k8s_node_2}=https://${ETCD_k8s_node_address_2}:${ETCD_peer_port}
>  --key-file=${ETCD_serv_key_path}
>  --listen-client-urls=https://127.0.0.1:${ETCD_serv_port},${ETCD_endpoint_2}
>  --listen-metrics-urls=http://0.0.0.0:${ETCD_metr_port}
>  --listen-peer-urls=https://${ETCD_k8s_node_address_2}:${ETCD_peer_port}
>  --name=${ETCD_k8s_node_2}
>  --peer-cert-file=${ETCD_peer_cert_path}
>  --peer-client-cert-auth=true
>  --peer-key-file=${ETCD_peer_key_path}
>  --peer-trusted-ca-file=${ETCD_ca_cert_path}
>  --trusted-ca-file=${ETCD_ca_key_path}
>`}
>    </CodeBlock>
>
>    <table>
>      <tr><th>Параметр</th><th>Описание</th></tr>
>      <tr><td>`advertise-client-urls`       </td><td>Анонсируемые точки подключения к узлу, для подключения клиентов. Совпадает с IP адресом узла k8s кластера</td></tr>
>      <tr><td>`cert-file`                   </td><td>Путь до сертификата ETCD кластера</td></tr>
>      <tr><td>`client-cert-auth`            </td><td>Включить/выключить авторизацию пользователей по сертификату</td></tr>
>      <tr><td>`data-dir`                    </td><td>Директория в которой хранятся файлы БД ETCD</td></tr>
>      <tr><td>`key-file`                    </td><td>Путь до ключа сертификата ETCD кластера</td></tr>
>      <tr><td>`initial-advertise-peer-urls` </td><td>Анонсируемые точки подключения к данному узлу для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`initial-cluster`             </td><td>Перечень узлов кластера ETCD и их точки подключения для взаимодействия между узлами ETCD кластера, которые будут добавлены в метаданные БД при запуске текущего узла.</td></tr>
>      <tr><td>`listen-client-urls`          </td><td>Точка подключения, на которой ETCD будет принимать клиентские запросы (`0.0.0.0`, `127.0.0.1` или IP адрес узла k8s кластера)</td></tr>
>      <tr><td>`listen-metrics-urls`         </td><td>Точка подключения, на которой ETCD будет принимать запросы к метрикам (`0.0.0.0`, `127.0.0.1` или IP адрес узла k8s кластера)</td></tr>
>      <tr><td>`listen-peer-urls`            </td><td>Точка подключения, на которой ETCD будет принимать запросы от других узлов ETCD кластера (`0.0.0.0`, `127.0.0.1` или IP адрес узла k8s кластера)</td></tr>
>      <tr><td>`name`                        </td><td>Имя узла ETCD кластера</td></tr>
>      <tr><td>`peer-cert-file`              </td><td>Путь до сертификата для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`peer-client-cert-auth`       </td><td>Путь до сертификата для авторизации подключений между узлами ETCD кластера</td></tr>
>      <tr><td>`peer-key-file`               </td><td>Путь до ключа сертификата для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`peer-trusted-ca-file`        </td><td>Путь до корневого доверенного сертификата для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`trusted-ca-file`             </td><td>Путь до корневого доверенного сертификата ETCD кластера</td></tr>
>    </table>
>
>    7. Запустите kubelet:
>
>    <CodeBlock language="bash">{`systemctl start kubelet`}</CodeBlock>
>
>    8. Проверьте состояние контейнера и наличие ошибок
>
>    <CodeBlock language="bash">
>{`export CONTAINER_ID="$(crictl ps -a  \
>    --label io.kubernetes.container.name=etcd \
>    --label io.kubernetes.pod.namespace=kube-system \
>    | awk 'NR>1{r=$1} $0~/Running/{exit} END{print r}')"
>
>  crictl ps --id $CONTAINER_ID
>
>  crictl logs $CONTAINER_ID
>`}
>    </CodeBlock>
>
>    9. Вернитесь к первому узлу кластера и проверьте, что второй узел определился (Его состояние должно быть `started`):
>
>    <CodeBlock language="bash">{`ectl member list -w table`}</CodeBlock>
>
>    10. После этого переведите второй узел в рабочий режим:
>
>    <CodeBlock language="bash">{`ectl member promote \${K8S_NODE_2}`}</CodeBlock>
>
>    11. При помощи команд `member list` и `endpoint status` описанных выше [проверьте состояние кластера](#проверка-подключения-и-состояния-кластера).
>
>    12. Переходите к третьему узла кластера
>  </TabItem>
>  <TabItem value='Master-3'>
>
>    1. Подключитесь к первому узлу ETCD кластера и добавьте третий узел кластера в режиме обучающегося:
>
>    <CodeBlock language="bash">{`etcdctl member add \${K8S_NODE_3} --peer-urls=https://\${K8S_NODE_ADDRESS_3}:2380 --learner ;`}</CodeBlock>
>
>    2. Подключитесь к ВМ  третьего узла кластера по ssh и остановите `kubelet`, чтобы не позволить ему запускать ETCD.
>
>    <CodeBlock language="bash">{`systemctl stop kubelet`}</CodeBlock>
>
>    3. Остановите и удалите контейнер `etcd`:
>
>    <CodeBlock language="bash">
>{`export CONTAINER_ID="$(crictl ps -a  \\
>    --label io.kubernetes.container.name=etcd \\
>    --label io.kubernetes.pod.namespace=kube-system \\
>    | awk 'NR>1{r=$1} $0~/Running/{exit} END{print r}')"
>  crictl stop \${CONTAINER_ID}
>  crictl rm \${CONTAINER_ID}
>`}
>    </CodeBlock>
>
>    4. Создайте [резервную копию](#создание-резервной-копии-файлов-бд).
>
>    5. Удалите старые файлы из директории <code>{ETCD_data_dir}</code>
>
>    <CodeBlock language="bash">{`rm -rf ${ETCD_workdir}/*`}</CodeBlock>
>
>    6. Откройте на редактирование файл <code>${ETCD_base_k8s_path}/manifests/etcd.yaml</code>:
>    Найдите и замените значение следующих ключей в разделе `spec.containers[0].command` аналогично примеру ниже
>    в соответствии с вашими параметрамии таблицей с описанием:
>
>    <CodeBlock language="bash">
>{`--advertise-client-urls=${ETCD_endpoint_3}
>  --cert-file=${ETCD_serv_cert_path}
>  --client-cert-auth=true
>  --data-dir=${ETCD_data_dir}
>  --initial-advertise-peer-urls=https://${ETCD_k8s_node_address_3}:${ETCD_peer_port}
>  --initial-cluster=${ETCD_k8s_node_1}=https://${ETCD_k8s_node_address_1}:${ETCD_peer_port},${ETCD_k8s_node_2}=https://${ETCD_k8s_node_address_2}:${ETCD_peer_port},${ETCD_k8s_node_3}=https://${ETCD_k8s_node_address_3}:${ETCD_peer_port}
>  --key-file=${ETCD_serv_key_path}
>  --listen-client-urls=https://127.0.0.1:${ETCD_serv_port},${ETCD_endpoint_3}
>  --listen-metrics-urls=http://0.0.0.0:${ETCD_metr_port}
>  --listen-peer-urls=https://${ETCD_k8s_node_address_3}:${ETCD_peer_port}
>  --name=${ETCD_k8s_node_3}
>  --peer-cert-file=${ETCD_peer_cert_path}
>  --peer-client-cert-auth=true
>  --peer-key-file=${ETCD_peer_key_path}
>  --peer-trusted-ca-file=${ETCD_ca_cert_path}
>  --trusted-ca-file=${ETCD_ca_key_path}
>`}
>    </CodeBlock>
>
>    <table>
>      <tr><th>Параметр</th><th>Описание</th></tr>
>      <tr><td>`advertise-client-urls`       </td><td>Анонсируемые точки подключения к узлу, для подключения клиентов. Совпадает с IP адресом узла k8s кластера</td></tr>
>      <tr><td>`cert-file`                   </td><td>Путь до сертификата ETCD кластера</td></tr>
>      <tr><td>`client-cert-auth`            </td><td>Включить/выключить авторизацию пользователей по сертификату</td></tr>
>      <tr><td>`data-dir`                    </td><td>Директория в которой хранятся файлы БД ETCD</td></tr>
>      <tr><td>`key-file`                    </td><td>Путь до ключа сертификата ETCD кластера</td></tr>
>      <tr><td>`initial-advertise-peer-urls` </td><td>Анонсируемые точки подключения к данному узлу для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`initial-cluster`             </td><td>Перечень узлов кластера ETCD и их точки подключения для взаимодействия между узлами ETCD кластера, которые будут добавлены в метаданные БД при запуске текущего узла.</td></tr>
>      <tr><td>`listen-client-urls`          </td><td>Точка подключения, на которой ETCD будет принимать клиентские запросы (`0.0.0.0`, `127.0.0.1` или IP адрес узла k8s кластера)</td></tr>
>      <tr><td>`listen-metrics-urls`         </td><td>Точка подключения, на которой ETCD будет принимать запросы к метрикам (`0.0.0.0`, `127.0.0.1` или IP адрес узла k8s кластера)</td></tr>
>      <tr><td>`listen-peer-urls`            </td><td>Точка подключения, на которой ETCD будет принимать запросы от других узлов ETCD кластера (`0.0.0.0`, `127.0.0.1` или IP адрес узла k8s кластера)</td></tr>
>      <tr><td>`name`                        </td><td>Имя узла ETCD кластера</td></tr>
>      <tr><td>`peer-cert-file`              </td><td>Путь до сертификата для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`peer-client-cert-auth`       </td><td>Путь до сертификата для авторизации подключений между узлами ETCD кластера</td></tr>
>      <tr><td>`peer-key-file`               </td><td>Путь до ключа сертификата для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`peer-trusted-ca-file`        </td><td>Путь до корневого доверенного сертификата для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`trusted-ca-file`             </td><td>Путь до корневого доверенного сертификата ETCD кластера</td></tr>
>    </table>
>
>
>    7. Запустите kubelet:
>
>    <CodeBlock language="bash">{`systemctl start kubelet`}</CodeBlock>
>
>    8. Проверьте состояние контейнера и наличие ошибок
>
>    <CodeBlock language="bash">
>{`export CONTAINER_ID="$(crictl ps -a  \
>    --label io.kubernetes.container.name=etcd \
>    --label io.kubernetes.pod.namespace=kube-system \
>    | awk 'NR>1{r=$1} $0~/Running/{exit} END{print r}')"
>
>  crictl ps --id $CONTAINER_ID
>
>  crictl logs $CONTAINER_ID
>`}
>    </CodeBlock>
>
>    9. Вернитесь к первому узлу кластера и проверьте, что третий узел определился (Его состояние должно быть `started`):
>
>    <CodeBlock language="bash">{`ectl member list -w table`}</CodeBlock>
>
>    10. После этого переведите третий узел в рабочий режим:
>
>    <CodeBlock language="bash">{`ectl member promote \${K8S_NODE_3}`}</CodeBlock>
>
>    11. При помощи команд `member list` и `endpoint status` описанных выше [проверьте состояние кластера](#проверка-подключения-и-состояния-кластера).
>
>  </TabItem>
></Tabs>

После выполнения работ на всех трех узлах ETCD кластера, кластер восстановлен и готов к работе.

*****

### Восстановление всех трех узлов из мгновенного снимка

Данный вариант подразумевает, восстановление БД и ее метаданных на всех трех узлах одновременно.

:::warning
В данном случае, если при запуске восстановленного узла ETCD кластера, в его конфигурации будут присутствовать
точки подключения и имена других узлов, которые не сконфигурированы для работы с восстановленным узлом,
тогда под восстановленного узла может падать с ошибкой, до тех пор, пока на других узлах не появится корректная конфигурация.
:::

Шаги с 1 по 8 рекомендуется выполнять синхронно на всех трех узлах ETCD кластера.

><Tabs groupId="etcd-node">
>  <TabItem value='Master-1'>
>    1. Загрузите файл мгновенного снимка на ВМ первого узла кластера по пути <code>{ETCD_workdir}/backup.db</code> при помощи rsync
>
>    <CodeBlock language="bash">
>{`ssh capv@$\{K8S_NODE_ADDRESS_1} "mkdir -p ${ETCD_workdir}"
>  rsync --rsync-path="sudo rsync" \\
>    LOCAL_PATH_TO_BACKUP/backup.db \\
>    capv@$\{K8S_NODE_ADDRESS_1}:${ETCD_workdir}/backup.db
>`}
>    </CodeBlock>
>
>    2. Подключитесь к ВМ первого узла кластера по ssh и переключитесь на суперпользователя.
>    Затем остановите `kubelet`, чтобы не позволить ему запускать ETCD.
>
>    <CodeBlock language="bash">{`systemctl stop kubelet`}</CodeBlock>
>
>    3. Остановите и удалите контейнер `etcd`:
>
>    <CodeBlock language="bash">
>{`export CONTAINER_ID="$(crictl ps -a  \\
>    --label io.kubernetes.container.name=etcd \\
>    --label io.kubernetes.pod.namespace=kube-system \\
>    | awk 'NR>1{r=$1} $0~/Running/{exit} END{print r}')"
>  crictl stop \${CONTAINER_ID}
>  crictl rm \${CONTAINER_ID}
>`}
>    </CodeBlock>
>
>    4. Создайте [резервную копию](#создание-резервной-копии-файлов-бд).
>
>    5. Удалите старые файлы из директории <code>{ETCD_data_dir}</code>
>
>    <CodeBlock language="bash">{`rm -rf ${ETCD_data_dir}/*`}</CodeBlock>
>
>    6. Подготовьте БД из снимка:
>    <CodeBlock language="bash">
>{`etcdctl snapshot restore ${ETCD_workdir}/backup.db \\
>    --name=\${K8S_NODE_1} \\
>    --data-dir ${ETCD_data_dir} \\
>    --initial-advertise-peer-urls=https://\${K8S_NODE_ADDRESS_1}:2380 \\
>    --initial-cluster=\${K8S_NODE_1}=https://\${K8S_NODE_ADDRESS_1}:${ETCD_peer_port},\${K8S_NODE_2}=https://\${K8S_NODE_ADDRESS_2}:${ETCD_peer_port},\${K8S_NODE_3}=https://\${K8S_NODE_ADDRESS_3}:${ETCD_peer_port}
>`}
>    </CodeBlock>
>
>    :::note
>    Обратите внимание, что запускается локальный файл `etcdctl` и снимок так же расположен на мастере в директории <code>{ETCD_workdir}</code>.
>    В качестве `data-dir` указана директория на хосте, которая монтируется внутрь контейнера ETCD и будет использоваться в качестве хранения файлов БД.
>    :::
>
>    7. Проверьте и по необходимости отредактируйте параметры запуска ETCD в файле <code>${ETCD_base_k8s_path}/manifests/etcd.yaml</code>
>    указанные в разделе `spec.containers[0].command`.
>    А именно проверьте наличие необходимых полей и их значения, поля не входящий в данный перечень удалять не нужно!
>
>    <CodeBlock language="bash">
>{`--advertise-client-urls=${ETCD_endpoint_1}
>  --cert-file=${ETCD_serv_cert_path}
>  --client-cert-auth=true
>  --data-dir=${ETCD_data_dir}
>  --key-file=${ETCD_serv_key_path}
>  --listen-client-urls=https://127.0.0.1:${ETCD_serv_port},${ETCD_endpoint_1}
>  --listen-metrics-urls=http://0.0.0.0:${ETCD_metr_port}
>  --listen-peer-urls=https://${ETCD_k8s_node_address_1}:${ETCD_peer_port}
>  --name=${ETCD_k8s_node_1}
>  --peer-cert-file=${ETCD_peer_cert_path}
>  --peer-client-cert-auth=true
>  --peer-key-file=${ETCD_peer_key_path}
>  --peer-trusted-ca-file=${ETCD_ca_cert_path}
>  --trusted-ca-file=${ETCD_ca_key_path}
>`}
>    </CodeBlock>
>
>    <table>
>      <tr><th>Параметр</th><th>Описание</th></tr>
>      <tr><td>`advertise-client-urls` </td><td>Анонсируемые точки подключения к узлу, для подключения клиентов. Совпадает с IP адресом узла k8s кластера</td></tr>
>      <tr><td>`cert-file`             </td><td>Путь до сертификата ETCD кластера</td></tr>
>      <tr><td>`client-cert-auth`      </td><td>Включить/выключить авторизацию пользователей по сертификату</td></tr>
>      <tr><td>`data-dir`              </td><td>Директория в которой хранятся файлы БД ETCD</td></tr>
>      <tr><td>`key-file`              </td><td>Путь до ключа сертификата ETCD кластера</td></tr>
>      <tr><td>`listen-client-urls`    </td><td>Точка подключения, на которой ETCD будет принимать клиентские запросы (`0.0.0.0`, `127.0.0.1` или IP адрес узла k8s кластера)</td></tr>
>      <tr><td>`listen-metrics-urls`   </td><td>Точка подключения, на которой ETCD будет принимать запросы к метрикам (`0.0.0.0`, `127.0.0.1` или IP адрес узла k8s кластера)</td></tr>
>      <tr><td>`listen-peer-urls`      </td><td>Точка подключения, на которой ETCD будет принимать запросы от других узлов ETCD кластера (`0.0.0.0`, `127.0.0.1` или IP адрес узла k8s кластера)</td></tr>
>      <tr><td>`name`                  </td><td>Имя узла ETCD кластера</td></tr>
>      <tr><td>`peer-cert-file`        </td><td>Путь до сертификата для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`peer-client-cert-auth` </td><td>Путь до сертификата для авторизации подключений между узлами ETCD кластера</td></tr>
>      <tr><td>`peer-key-file`         </td><td>Путь до ключа сертификата для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`peer-trusted-ca-file`  </td><td>Путь до корневого доверенного сертификата для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`trusted-ca-file`       </td><td>Путь до корневого доверенного сертификата ETCD кластера</td></tr>
>    </table>
>
>    :::note
>    Если параметр `initial-cluster-state` равен значению "existing" или отсутствуют файлы БД и метаданных в ней,
>    тогда все прочие параметры, имя которых начинается со слова `initial` будут игнорироваться.
>    В нашем случае, мы уже восстановили файлы БД и внесли метаданные на этапе подготовки БД из снимка.
>    :::
>
>    8. Выполните аналогичные действия на остальных узлах ETCD кластера.
>
>    9. Запустите kubelet:
>
>    <CodeBlock language="bash">{`systemctl start kubelet`}</CodeBlock>
>
>    10. Проверьте состояние контейнера и наличие ошибок
>
>    <CodeBlock language="bash">
>{`export CONTAINER_ID="$(crictl ps -a  \\
>    --label io.kubernetes.container.name=etcd \\
>    --label io.kubernetes.pod.namespace=kube-system \\
>    | awk 'NR>1{r=$1} $0~/Running/{exit} END{print r}')"
>
>  crictl ps --id $CONTAINER_ID
>
>  crictl logs $CONTAINER_ID
>`}
>    </CodeBlock>
>  </TabItem>
>  <TabItem value='Master-2'>
>    1. Загрузите файл мгновенного снимка на ВМ первого узла кластера по пути <code>{ETCD_workdir}/backup.db</code> при помощи rsync
>
>    <CodeBlock language="bash">
>{`ssh capv@$\{K8S_NODE_ADDRESS_1} "mkdir -p ${ETCD_workdir}"
>  rsync --rsync-path="sudo rsync" \\
>    LOCAL_PATH_TO_BACKUP/backup.db \\
>    capv@$\{K8S_NODE_ADDRESS_1}:${ETCD_workdir}/backup.db
>`}
>    </CodeBlock>
>
>    2. Подключитесь к ВМ первого узла кластера по ssh и переключитесь на суперпользователя.
>    Затем остановите `kubelet`, чтобы не позволить ему запускать ETCD.
>
>    <CodeBlock language="bash">{`systemctl stop kubelet`}</CodeBlock>
>
>    3. Остановите и удалите контейнер `etcd`:
>
>    <CodeBlock language="bash">
>{`export CONTAINER_ID="$(crictl ps -a  \\
>    --label io.kubernetes.container.name=etcd \\
>    --label io.kubernetes.pod.namespace=kube-system \\
>    | awk 'NR>1{r=$1} $0~/Running/{exit} END{print r}')"
>  crictl stop \${CONTAINER_ID}
>  crictl rm \${CONTAINER_ID}
>`}
>    </CodeBlock>
>
>    4. Создайте [резервную копию](#создание-резервной-копии-файлов-бд).
>
>    5. Удалите старые файлы из директории <code>{ETCD_data_dir}</code>
>
>    <CodeBlock language="bash">{`rm -rf ${ETCD_data_dir}/*`}</CodeBlock>
>
>    6. Подготовьте БД из снимка:
>    <CodeBlock language="bash">
>{`etcdctl snapshot restore ${ETCD_workdir}/backup.db \\
>    --name=\${K8S_NODE_1} \\
>    --data-dir ${ETCD_data_dir} \\
>    --initial-advertise-peer-urls=https://\${K8S_NODE_ADDRESS_1}:2380 \\
>    --initial-cluster=\${K8S_NODE_1}=https://\${K8S_NODE_ADDRESS_1}:${ETCD_peer_port},\${K8S_NODE_2}=https://\${K8S_NODE_ADDRESS_2}:${ETCD_peer_port},\${K8S_NODE_3}=https://\${K8S_NODE_ADDRESS_3}:${ETCD_peer_port}
>`}
>    </CodeBlock>
>
>    :::note
>    Обратите внимание, что запускается локальный файл `etcdctl` и снимок так же расположен на мастере в директории <code>{ETCD_workdir}</code>.
>    В качестве `data-dir` указана директория на хосте, которая монтируется внутрь контейнера ETCD и будет использоваться в качестве хранения файлов БД.
>    :::
>
>    7. Проверьте и по необходимости отредактируйте параметры запуска ETCD в файле <code>${ETCD_base_k8s_path}/manifests/etcd.yaml</code>
>    указанные в разделе `spec.containers[0].command`.
>    А именно проверьте наличие необходимых полей и их значения, поля не входящий в данный перечень удалять не нужно!
>
>    <CodeBlock language="bash">
>{`--advertise-client-urls=${ETCD_endpoint_1}
>  --cert-file=${ETCD_serv_cert_path}
>  --client-cert-auth=true
>  --data-dir=${ETCD_data_dir}
>  --key-file=${ETCD_serv_key_path}
>  --listen-client-urls=https://127.0.0.1:${ETCD_serv_port},${ETCD_endpoint_1}
>  --listen-metrics-urls=http://0.0.0.0:${ETCD_metr_port}
>  --listen-peer-urls=https://${ETCD_k8s_node_address_1}:${ETCD_peer_port}
>  --name=${ETCD_k8s_node_1}
>  --peer-cert-file=${ETCD_peer_cert_path}
>  --peer-client-cert-auth=true
>  --peer-key-file=${ETCD_peer_key_path}
>  --peer-trusted-ca-file=${ETCD_ca_cert_path}
>  --trusted-ca-file=${ETCD_ca_key_path}
>`}
>    </CodeBlock>
>
>    <table>
>      <tr><th>Параметр</th><th>Описание</th></tr>
>      <tr><td>`advertise-client-urls` </td><td>Анонсируемые точки подключения к узлу, для подключения клиентов. Совпадает с IP адресом узла k8s кластера</td></tr>
>      <tr><td>`cert-file`             </td><td>Путь до сертификата ETCD кластера</td></tr>
>      <tr><td>`client-cert-auth`      </td><td>Включить/выключить авторизацию пользователей по сертификату</td></tr>
>      <tr><td>`data-dir`              </td><td>Директория в которой хранятся файлы БД ETCD</td></tr>
>      <tr><td>`key-file`              </td><td>Путь до ключа сертификата ETCD кластера</td></tr>
>      <tr><td>`listen-client-urls`    </td><td>Точка подключения, на которой ETCD будет принимать клиентские запросы (`0.0.0.0`, `127.0.0.1` или IP адрес узла k8s кластера)</td></tr>
>      <tr><td>`listen-metrics-urls`   </td><td>Точка подключения, на которой ETCD будет принимать запросы к метрикам (`0.0.0.0`, `127.0.0.1` или IP адрес узла k8s кластера)</td></tr>
>      <tr><td>`listen-peer-urls`      </td><td>Точка подключения, на которой ETCD будет принимать запросы от других узлов ETCD кластера (`0.0.0.0`, `127.0.0.1` или IP адрес узла k8s кластера)</td></tr>
>      <tr><td>`name`                  </td><td>Имя узла ETCD кластера</td></tr>
>      <tr><td>`peer-cert-file`        </td><td>Путь до сертификата для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`peer-client-cert-auth` </td><td>Путь до сертификата для авторизации подключений между узлами ETCD кластера</td></tr>
>      <tr><td>`peer-key-file`         </td><td>Путь до ключа сертификата для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`peer-trusted-ca-file`  </td><td>Путь до корневого доверенного сертификата для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`trusted-ca-file`       </td><td>Путь до корневого доверенного сертификата ETCD кластера</td></tr>
>    </table>
>
>    :::note
>    Если параметр `initial-cluster-state` равен значению "existing" или отсутствуют файлы БД и метаданных в ней,
>    тогда все прочие параметры, имя которых начинается со слова `initial` будут игнорироваться.
>    В нашем случае, мы уже восстановили файлы БД и внесли метаданные на этапе подготовки БД из снимка.
>    :::
>
>    8. Выполните аналогичные действия на остальных узлах ETCD кластера.
>
>    9. Запустите kubelet:
>
>    <CodeBlock language="bash">{`systemctl start kubelet`}</CodeBlock>
>
>    10. Проверьте состояние контейнера и наличие ошибок
>
>    <CodeBlock language="bash">
>{`export CONTAINER_ID="$(crictl ps -a  \\
>    --label io.kubernetes.container.name=etcd \\
>    --label io.kubernetes.pod.namespace=kube-system \\
>    | awk 'NR>1{r=$1} $0~/Running/{exit} END{print r}')"
>
>  crictl ps --id $CONTAINER_ID
>
>  crictl logs $CONTAINER_ID
>`}
>    </CodeBlock>
>  </TabItem>
>  <TabItem value='Master-3'>
>    1. Загрузите файл мгновенного снимка на ВМ первого узла кластера по пути <code>{ETCD_workdir}/backup.db</code> при помощи rsync
>
>    <CodeBlock language="bash">
>{`ssh capv@$\{K8S_NODE_ADDRESS_1} "mkdir -p ${ETCD_workdir}"
>  rsync --rsync-path="sudo rsync" \\
>    LOCAL_PATH_TO_BACKUP/backup.db \\
>    capv@$\{K8S_NODE_ADDRESS_1}:${ETCD_workdir}/backup.db
>`}
>    </CodeBlock>
>
>    2. Подключитесь к ВМ первого узла кластера по ssh и переключитесь на суперпользователя.
>    Затем остановите `kubelet`, чтобы не позволить ему запускать ETCD.
>
>    <CodeBlock language="bash">{`systemctl stop kubelet`}</CodeBlock>
>
>    3. Остановите и удалите контейнер `etcd`:
>
>    <CodeBlock language="bash">
>{`export CONTAINER_ID="$(crictl ps -a  \\
>    --label io.kubernetes.container.name=etcd \\
>    --label io.kubernetes.pod.namespace=kube-system \\
>    | awk 'NR>1{r=$1} $0~/Running/{exit} END{print r}')"
>  crictl stop \${CONTAINER_ID}
>  crictl rm \${CONTAINER_ID}
>`}
>    </CodeBlock>
>
>    4. Создайте [резервную копию](#создание-резервной-копии-файлов-бд).
>
>    5. Удалите старые файлы из директории <code>{ETCD_data_dir}</code>
>
>    <CodeBlock language="bash">{`rm -rf ${ETCD_data_dir}/*`}</CodeBlock>
>
>    6. Подготовьте БД из снимка:
>    <CodeBlock language="bash">
>{`etcdctl snapshot restore ${ETCD_workdir}/backup.db \\
>    --name=\${K8S_NODE_1} \\
>    --data-dir ${ETCD_data_dir} \\
>    --initial-advertise-peer-urls=https://\${K8S_NODE_ADDRESS_1}:2380 \\
>    --initial-cluster=\${K8S_NODE_1}=https://\${K8S_NODE_ADDRESS_1}:${ETCD_peer_port},\${K8S_NODE_2}=https://\${K8S_NODE_ADDRESS_2}:${ETCD_peer_port},\${K8S_NODE_3}=https://\${K8S_NODE_ADDRESS_3}:${ETCD_peer_port}
>`}
>    </CodeBlock>
>
>    :::note
>    Обратите внимание, что запускается локальный файл `etcdctl` и снимок так же расположен на мастере в директории <code>{ETCD_workdir}</code>.
>    В качестве `data-dir` указана директория на хосте, которая монтируется внутрь контейнера ETCD и будет использоваться в качестве хранения файлов БД.
>    :::
>
>    7. Проверьте и по необходимости отредактируйте параметры запуска ETCD в файле <code>${ETCD_base_k8s_path}/manifests/etcd.yaml</code>
>    указанные в разделе `spec.containers[0].command`.
>    А именно проверьте наличие необходимых полей и их значения, поля не входящий в данный перечень удалять не нужно!
>
>    <CodeBlock language="bash">
>{`--advertise-client-urls=${ETCD_endpoint_1}
>  --cert-file=${ETCD_serv_cert_path}
>  --client-cert-auth=true
>  --data-dir=${ETCD_data_dir}
>  --key-file=${ETCD_serv_key_path}
>  --listen-client-urls=https://127.0.0.1:${ETCD_serv_port},${ETCD_endpoint_1}
>  --listen-metrics-urls=http://0.0.0.0:${ETCD_metr_port}
>  --listen-peer-urls=https://${ETCD_k8s_node_address_1}:${ETCD_peer_port}
>  --name=${ETCD_k8s_node_1}
>  --peer-cert-file=${ETCD_peer_cert_path}
>  --peer-client-cert-auth=true
>  --peer-key-file=${ETCD_peer_key_path}
>  --peer-trusted-ca-file=${ETCD_ca_cert_path}
>  --trusted-ca-file=${ETCD_ca_key_path}
>`}
>    </CodeBlock>
>
>    <table>
>      <tr><th>Параметр</th><th>Описание</th></tr>
>      <tr><td>`advertise-client-urls` </td><td>Анонсируемые точки подключения к узлу, для подключения клиентов. Совпадает с IP адресом узла k8s кластера</td></tr>
>      <tr><td>`cert-file`             </td><td>Путь до сертификата ETCD кластера</td></tr>
>      <tr><td>`client-cert-auth`      </td><td>Включить/выключить авторизацию пользователей по сертификату</td></tr>
>      <tr><td>`data-dir`              </td><td>Директория в которой хранятся файлы БД ETCD</td></tr>
>      <tr><td>`key-file`              </td><td>Путь до ключа сертификата ETCD кластера</td></tr>
>      <tr><td>`listen-client-urls`    </td><td>Точка подключения, на которой ETCD будет принимать клиентские запросы (`0.0.0.0`, `127.0.0.1` или IP адрес узла k8s кластера)</td></tr>
>      <tr><td>`listen-metrics-urls`   </td><td>Точка подключения, на которой ETCD будет принимать запросы к метрикам (`0.0.0.0`, `127.0.0.1` или IP адрес узла k8s кластера)</td></tr>
>      <tr><td>`listen-peer-urls`      </td><td>Точка подключения, на которой ETCD будет принимать запросы от других узлов ETCD кластера (`0.0.0.0`, `127.0.0.1` или IP адрес узла k8s кластера)</td></tr>
>      <tr><td>`name`                  </td><td>Имя узла ETCD кластера</td></tr>
>      <tr><td>`peer-cert-file`        </td><td>Путь до сертификата для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`peer-client-cert-auth` </td><td>Путь до сертификата для авторизации подключений между узлами ETCD кластера</td></tr>
>      <tr><td>`peer-key-file`         </td><td>Путь до ключа сертификата для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`peer-trusted-ca-file`  </td><td>Путь до корневого доверенного сертификата для подключения между узлами ETCD кластера</td></tr>
>      <tr><td>`trusted-ca-file`       </td><td>Путь до корневого доверенного сертификата ETCD кластера</td></tr>
>    </table>
>
>    :::note
>    Если параметр `initial-cluster-state` равен значению "existing" или отсутствуют файлы БД и метаданных в ней,
>    тогда все прочие параметры, имя которых начинается со слова `initial` будут игнорироваться.
>    В нашем случае, мы уже восстановили файлы БД и внесли метаданные на этапе подготовки БД из снимка.
>    :::
>
>    8. Выполните аналогичные действия на остальных узлах ETCD кластера.
>
>    9. Запустите kubelet:
>
>    <CodeBlock language="bash">{`systemctl start kubelet`}</CodeBlock>
>
>    10. Проверьте состояние контейнера и наличие ошибок
>
>    <CodeBlock language="bash">
>{`export CONTAINER_ID="$(crictl ps -a  \\
>    --label io.kubernetes.container.name=etcd \\
>    --label io.kubernetes.pod.namespace=kube-system \\
>    | awk 'NR>1{r=$1} $0~/Running/{exit} END{print r}')"
>
>  crictl ps --id $CONTAINER_ID
>
>  crictl logs $CONTAINER_ID
>`}
>    </CodeBlock>
>  </TabItem>
></Tabs>

11. Повторите этапы сбора [пререквизитов](#подготовка-окружения) и [подключения](#подключение-к-etcd) к кластеру ETCD.

12. При помощи команд `member list` и `endpoint status` описанных выше [проверьте состояние кластера](#проверка-подключения-и-состояния-кластера).

После выполнения работ на всех трех узлах ETCD кластера, кластер восстановлен и готов к работе.

******

### Восстановление кластера при конфликтах или проблемах с кворумом

Данная необходимость может возникнуть в случаях:

I. При обновлении кластера появилось больше или меньше 3х управляющих узлов K8s кластера.
II. Распался кворум ETCD кластера.
III. Повреждены метаданные БД на одном или нескольких узлах ETCD кластера.

1. Определите текущее количество узлов ETCD кластера.

><Tabs groupId="etcd-kubeapi">
>  <TabItem value='KubeAPI восстанавливаемого кластера'>
>    Если  KubeAPI восстанавливаемого  кластера доступно, тогда используйте этот вариант.
>
>    2. Подключитесь к KubeAPI и введите команду:
>    <CodeBlock language="bash">{`kubectl get pod -n kube-system -l component=etcd -o wide`}</CodeBlock>
>
>    3. Выберите под узла ETCD кластера из полученной таблицы, на основе которого вы будете восстанавливать кластер.
>
>  </TabItem>
>  <TabItem value='kubeAPI инфра кластера'>
>    2. Подключитесь к KubeAPI инфраструктурного кластера и введите команду:
>
>    <CodeBlock language="bash">
>{`kubectl get machine -n bcloud-k8s-sandbox-test-csko-1
>  -l cluster.x-k8s.io/control-plane=
>  -o custom-columns=NAME:.metadata.name,ADDRESSES:status.addresses\[0\].address
>`}
>    </CodeBlock>
>    3. Выберите узел k8s кластера, с которого вы будете восстанавливать кластер.
>  </TabItem>
></Tabs>

4. Подключитесь к выбранному вами узлу K8s кластера или узлу K8s кластера выбранного вами пода.

5. Cоберите все возможные [пререквизиты](#подготовка-окружения) для подключения к ETCD и K8s кластеру.

><Tabs groupId="etcd-status">
>  <TabItem value='ETCD кластер работает'>
>    6. Если удается подключиться к ETCD кластеру, тогда создайте [резервную копию](#создание-резервных-копий)
>    и [выполните восстановление кластера](#восстановление-с-одного-узла-из-мгновенного-снимка) с дальнейшим подключением
>    остальных узлов ETCD кластера.
>  </TabItem>
>  <TabItem value='ETCD кластер не работает'>
>    6. Если подключение к ETCD кластеру невозможно, сделайте [резервную копию файлов БД](#создание-резервной-копии-файлов-бд).
>
>    7. Откройте на редактирование файл <code>${ETCD_base_k8s_path}/manifests/etcd.yaml</code>.
>    8. В параметрах запуска ETCD указанных в разделе `spec.containers[0].command`
>    добавьте ключ:
>    <CodeBlock language="bash">{`--force-new-cluster`}</CodeBlock>
>    Здесь же измените параметр `initial-cluster` так, чтобы в нем остался только текущий узел ETCD кластера.
>    <CodeBlock language="bash">{`\--initial-cluster=${ETCD_k8s_node_1}=https://${ETCD_k8s_node_address_1}:${ETCD_peer_port} `}</CodeBlock>
>    Таким образом, при следущем запуске ETCD будут удалены все текущие метаданные из файлов БД,
>    и туда будут записаны новые, указанные в разделе `spec.containers[0].command` с суффиксом initial.
>
>    9. Дождитесь, когда пересоздастся под ETCD узла и проверьте его состояние.
>
>    10. В файле <code>${ETCD_base_k8s_path}/manifests/etcd.yaml</code>
>    в параметрах запуска ETCD указанных в разделе `spec.containers[0].command`
>    удалите ключ:
>    <CodeBlock language="bash">{`--force-new-cluster`}</CodeBlock>
>    В противном случае, при каждом перезапуске пода/контейнера будут удаляться все метаданные из БД.
>
>    10. Добавьте 2й и 3й узлы кластера ETCD как это указано в инструкции к
>    [восстановлению с одного узла](#восстановление-с-одного-узла-из-мгновенного-снимка) для `Master-2` и `Master-3`.
>  </TabItem>
></Tabs>

На этом восстановление кластера завершено.

*****

TODO опиши как работает gardener etcd-backup (в режиме снапшотов)

*****
